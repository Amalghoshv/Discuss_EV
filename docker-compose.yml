version: "3.9"

services:
  db:
    image: postgres:15
    container_name: discuss_ev_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: discuss_ev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
